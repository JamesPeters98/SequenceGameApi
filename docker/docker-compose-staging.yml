x-db-env: &db-env
  POSTGRES_DB: "sequencegame"
  POSTGRES_USER: "sequencegame"
  POSTGRES_PASSWORD: "sequencegame_staging_password"

services:
  postgres:
    image: postgres:18.1
    environment: *db-env
    volumes:
      - pgdata_staging:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    image: sequence-game-staging-backend:latest
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      <<: *db-env
      SPRING_PROFILES_ACTIVE: "prod,staging"
      SEQUENCE_API_BEARER_TOKEN: "${SERVICE_PASSWORD_BACKEND}"

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: "${SERVICE_URL_BACKEND}"
        VITE_ALLOWED_HOSTS: "${SERVICE_FQDN_FRONTEND}"
        VITE_API_BEARER_TOKEN: "${SERVICE_PASSWORD_BACKEND}"
    image: sequence-game-staging-frontend:latest
    ports:
      - "4173:4173"
    depends_on:
      - backend

volumes:
  pgdata_staging:
